<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only test harness. Absent from release builds by construction. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".debug.TestNotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.satyashield.app.TEST_NOTIFY" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
